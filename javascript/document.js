// Document object. 
// Document object represent an entire page when page load in browser. 

console.log(document);

// Create an element
var divElement = document.createElement("div");
divElement.setAttribute("background", 'red') // set the background color red. 
